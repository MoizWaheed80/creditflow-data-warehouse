# Windows Authentication to a local SQL Server instance.
# Must be the first line before any [section] starts.
destination.mssql.credentials="mssql://localhost/CreditFlow_Raw?trusted_connection=yes&encrypt=no&driver=ODBC+Driver+18+for+SQL+Server"

# If dlt complains about missing username/password with Windows auth, add:
# [destination.mssql.credentials]
# username = ""
# password = ""
